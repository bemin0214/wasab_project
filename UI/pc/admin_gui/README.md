# AdminGUI

AdminGUI is currently served by `service/wasab_server/runtime/app` through the
`/camera-view` FastAPI route.

The source view for the current route lives in:

```text
service/wasab_server/wasab_web_service/service.py
```
